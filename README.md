# mfdl
simple shell script to get mediafire download links, nothing crazy.

### USAGE:
sh mfdl [dl] [mediafire-link]

**NOTE:**

the script will download files only if explicitly specified with 'dl' as first argument, otherwise will just output the link & exit.
